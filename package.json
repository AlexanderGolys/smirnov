{
  "name": "smirnov",
  "version": "1.0.0",
  "description": "Dependency-free sampling from custom CDFs and PDFs using the Smirnov transform (inverse transform sampling).",
  "type": "module",
  "main": "./src/index.js",
  "types": "./src/index.d.ts",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./src/index.d.ts",
      "import": "./src/index.js"
    }
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "test": "node --test",
    "lint": "node --check src/index.js",
    "prepublishOnly": "npm test && npm run lint"
  },
  "keywords": [
    "inverse-transform-sampling",
    "sampling",
    "sampler",
    "random",
    "statistics",
    "probability",
    "cdf",
    "pdf"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
